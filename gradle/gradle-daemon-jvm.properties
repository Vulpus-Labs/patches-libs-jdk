#This file selects the JVM that runs the Gradle daemon via toolchain criteria.
#Gradle auto-detects an installed JDK matching the version below (e.g. a SDKMAN
#Temurin install) instead of relying on a machine-specific absolute path.
toolchainVersion=21
