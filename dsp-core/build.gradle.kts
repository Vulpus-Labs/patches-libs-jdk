// dsp-core: shared, dependency-free DSP primitives (coefficient ramping,
// NaN/Inf sanitisation). Mirrors the role of the small shared helpers in the
// Rust `patches-dsp` crate that every filter kernel builds on.

description = "Shared DSP primitives for the patches-libs-jdk filter and oscillator ports"
