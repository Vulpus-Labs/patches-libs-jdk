# The Gradle daemon (and its Kotlin DSL compiler) must run on a JDK it
# understands; the machine default is GraalVM 26, which the bundled Kotlin
# cannot parse. Pin the daemon to Temurin 21 (HotSpot — GraalVM is avoided so
# benchmark numbers are representative of the host runtime). The *compile*
# toolchain is selected independently in build.gradle.kts and resolves to this
# same Temurin 21 via Gradle toolchain auto-detection of the SDKMAN install.
org.gradle.java.home=/Users/dominicfox/.sdkman/candidates/java/21.0.11-tem
org.gradle.caching=true
